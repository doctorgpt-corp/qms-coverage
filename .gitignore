# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
python/dist/
python/build/

# Editor
.vscode/
.idea/
.DS_Store

# Tooling
.claude/

# Node
node_modules/

# Top-level dist/ is committed: bun doesn't install devDeps for git
# dependencies, so we can't rely on `prepare` to rebuild on install.
# Each tag ships pre-built artifacts; rebuild before tagging.
